[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pantry"
version = "0.1.0"
description = "Minimal control plane for Trolley Agent runtimes"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "mcp>=2.0,<3",
  "pydantic-settings>=2.7,<3",
  "tortoise-orm>=1.1.8,<2",
  "uvicorn>=0.34,<1",
  "websockets>=15,<18",
]

[project.scripts]
trolley = "trolley.main:main"

[project.optional-dependencies]
dev = [
  "pytest>=8.3,<9",
  "ruff>=0.9,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pantry", "src/trolley"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
